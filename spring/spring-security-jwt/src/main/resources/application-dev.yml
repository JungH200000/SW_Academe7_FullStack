server:
  port: 7777
spring:
  application:
    name: spring-security-jwt
  security:
    user:
      name: user
      password: 1111
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kbdb
    username: master
    password: tiger
  jpa:
    hibernate:
      ddl-auto: update #개발 중일 때는 update, 배포시에는 none 으로 권장
      show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  logging:
    level:
      org.hibernate.SQL: debug
      org.hibernate.type: trace
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 1000MB
file:
  upload_dir: D:/devSource/Spring/uploads #upload dir
jwt:
  secret: "mytop_secret_key_123456_abcdefghijk" # 32바이트 이상이어야 함. 짧으면 에러남. jwt서명에 사용 시크릿 키
  access-token-expiration: 900000 # 15분 = 60*15*1000
  refresh-token-expiration: 3600000 # 1시간 (밀리초) = 60*60*1000
