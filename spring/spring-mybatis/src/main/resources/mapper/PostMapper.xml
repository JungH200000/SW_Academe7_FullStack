<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper
 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.spring_mybatis.post.mapper.PostMapper">
<!--    PostMapper인터페이스의 패키지명과 인터페이스명을 namespace에 기술-->
    <select id="testMyBatis" resultType="int">
    <!--     id에는 PostMapper인터페이스의 메서드명을 기술해야 함   -->
        select count(id) from posts
    </select>

    <select id="listPost" resultType="com.my.spring_mybatis.post.domain.PostDTO">
        select id,name,title,content,attach, date_format(wdate,'%Y-%m-%d') wdate from posts order by id desc
    </select>
</mapper>